# TODO — documentação (responsável: mantenedor)

O código é escrito por mim; a documentação fica para o mantenedor.

Última atualização: 2026-09-24 (após a implementação do cliente LLM, cache e CLI).

## Concluído

- [x] Docstrings de lab.llm.client, lab.llm.cache, lab.settings, lab.llm.costs e lab.cli (marcadas com TODO(docs) no código).
- [x] Textos de --help do CLI além das descrições de uma linha.
- [x] Documentar config/models.yaml: campos, base_url_env e por que URL e chave vivem só no .env (comentários no próprio arquivo).
- [x] README.md: visão geral, instalação (uv sync), configuração do .env, uso de lab chat, aviso de que não é aconselhamento jurídico. (issue #1)
- [x] CHANGELOG.md: entrada em Unreleased para o M0. (issue #1)
- [x] LICENSE (Apache-2.0). (issue #1)
- [x] ADR 0001: escolha de provedores (provedor principal + alternativas). (issue #10) — decisão provisória; os modelos concretos ficam em config/models.yaml.

## Pendente

- (nada — M0 documentado)
